#app/__init__.py


# pip freeze > requirements.txt     # once all dependencies are installed list them in the requirements.txt
# pip install -r requirements.txt   # install dependencies


print("Initializing app/__init__.py")

# Recreate the Virtual Environment (if needed)
# Delete the existing venv folder (if it exists).
# Create a new virtual environment:
#   python -m venv venv
# Activate the new environment and reinstall dependencies:
#   pip install -r requirements.txt
#==============================================================================
# Import necessary modules or functions:
#   from app.gui.main_window import MainWindow
#   from app.config.settings import APP_TITLE, APP_GEOMETRY



# Initialize package-level variables or settings
__version__ = "1.0.0"
__author__ = "Your Name"

